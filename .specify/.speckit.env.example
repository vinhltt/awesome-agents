# Spec Kit Configuration Example
# Copy this file to .speckit.env and customize for your project

# ============================================================
# REQUIRED CONFIGURATION
# ============================================================

# Allowed ticket prefixes (comma-separated, case-insensitive)
# Example: "aa,BB,PROJ,hotfix"
SPECKIT_PREFIX_LIST="aa"

# Default folder when not specified in ticket ID
# Example: "features", "tickets", "tasks"
SPECKIT_DEFAULT_FOLDER="features"

# Main branch name for base
# Example: "master", "main", "develop"
SPECKIT_MAIN_BRANCH="master"

# Root directory for specs
# Example: ".specify", ".spec", "specs"
SPECKIT_SPECS_ROOT=".specify"

# ============================================================
# OPTIONAL CONFIGURATION
# ============================================================

# Custom ticket format validation regex
# Default: ^([a-zA-Z]+/)?([a-zA-Z]+)-([0-9]+)$
# SPECKIT_TICKET_FORMAT="^([a-zA-Z]+/)?([a-zA-Z]+)-([0-9]+)$"

# Validation hook script path (relative to repo root)
# Example: ".specify/hooks/validate-ticket.sh"
# SPECKIT_VALIDATION_HOOK=""

# Hook timeout in seconds
# Default: 30
# SPECKIT_HOOK_TIMEOUT="30"

# Hook failure behavior: "exit" or "warn"
# Default: "exit"
# SPECKIT_HOOK_FAIL_BEHAVIOR="exit"

# ============================================================
# USAGE EXAMPLES
# ============================================================

# Default environment:
#   ./create-new-feature.sh aa-123 "Feature name"

# Specific environment:
#   SPECKIT_ENV=production ./create-new-feature.sh hotfix-999 "Fix"

# Debug mode:
#   SPECKIT_DEBUG=true ./create-new-feature.sh aa-456 "Test"

# ============================================================
# NOTES
# ============================================================

# 1. All SPECKIT_* variables are automatically loaded
# 2. Environment-specific configs: .speckit.env.{environment}
# 3. Validation hooks receive these variables:
#    - SPECKIT_HOOK_PREFIX
#    - SPECKIT_HOOK_NUMBER
#    - SPECKIT_HOOK_FOLDER
#    - SPECKIT_HOOK_PHASE
#    - SPECKIT_HOOK_TICKET_ID
