# Spec Kit Environment Configuration Template
# Copy this file to .speckit.env and customize for your project
#
# Usage:
#   cp .speckit.env.template .speckit.env
#   # Edit .speckit.env with your project settings

# ==============================================================================
# PROJECT IDENTIFICATION
# ==============================================================================

# Comma-separated list of allowed ticket prefixes (case-insensitive)
# Examples:
#   - Single prefix:  SPECKIT_PREFIX_LIST="aa"
#   - Multiple:       SPECKIT_PREFIX_LIST="aa,AL,PROJ"
#   - Wildcards:      SPECKIT_PREFIX_LIST="*" (allow any prefix)
#
# Default: aa
SPECKIT_PREFIX_LIST="aa"

# Default folder for features when no folder prefix specified
# Examples:
#   - /ut:specify aa-123  → .specify/features/aa-123
#   - /ut:specify test/aa-123 → .specify/test/aa-123
#
# Default: features
SPECKIT_DEFAULT_FOLDER="features"

# ==============================================================================
# GIT CONFIGURATION
# ==============================================================================

# Main branch name for creating feature branches
# Default: master
SPECKIT_MAIN_BRANCH="master"

# ==============================================================================
# DIRECTORY STRUCTURE
# ==============================================================================

# Root directory for all specs and features
# Default: .specify
SPECKIT_SPECS_ROOT=".specify"

# ==============================================================================
# TICKET FORMAT VALIDATION
# ==============================================================================

# Regex pattern for ticket ID validation
# Must capture 3 groups: (folder?)(prefix)(number)
#
# Default (case-insensitive): ^([a-zA-Z]+/)?([a-zA-Z]+)-([0-9]+)$
#   - Matches: aa-123, AA-123, AL-991, Test/PROJ-456
#
# Lowercase only (legacy):
#   ^([a-z]+/)?([a-z]+)-([0-9]+)$
#   - Does NOT match: AA-123, AL-991
#
SPECKIT_TICKET_FORMAT="^([a-zA-Z]+/)?([a-zA-Z]+)-([0-9]+)$"

# ==============================================================================
# VALIDATION HOOKS (Optional)
# ==============================================================================

# Path to validation hook script (relative to repo root)
# Hook receives: SPECKIT_HOOK_PREFIX, SPECKIT_HOOK_NUMBER, SPECKIT_HOOK_FOLDER
# Example: .specify/scripts/hooks/validate-ticket.sh
# SPECKIT_VALIDATION_HOOK=""

# Hook timeout in seconds
# Default: 30
SPECKIT_HOOK_TIMEOUT="30"

# Hook failure behavior: "exit" (stop) or "warn" (continue)
# Default: exit
SPECKIT_HOOK_FAIL_BEHAVIOR="exit"

# ==============================================================================
# DEBUG MODE
# ==============================================================================

# Enable debug logging
# Default: false
# SPECKIT_DEBUG="false"

# ==============================================================================
# EXAMPLE CONFIGURATIONS
# ==============================================================================

# Example 1: Simple project with single prefix "aa"
# ------------------------------------------------
# SPECKIT_PREFIX_LIST="aa"
# SPECKIT_DEFAULT_FOLDER="features"
# SPECKIT_MAIN_BRANCH="master"
# SPECKIT_TICKET_FORMAT="^([a-zA-Z]+/)?([a-zA-Z]+)-([0-9]+)$"

# Example 2: Multi-project template with uppercase prefixes (AL, AA, PROJ)
# -------------------------------------------------------------------------
# SPECKIT_PREFIX_LIST="aa,AL,PROJ"
# SPECKIT_DEFAULT_FOLDER="features"
# SPECKIT_MAIN_BRANCH="main"
# SPECKIT_TICKET_FORMAT="^([a-zA-Z0-9]+/)?([a-zA-Z]+)-([0-9]+)$"

# Example 3: Accept any prefix (wildcard)
# ----------------------------------------
# SPECKIT_PREFIX_LIST="*"
# SPECKIT_DEFAULT_FOLDER="features"
# SPECKIT_MAIN_BRANCH="main"
# SPECKIT_TICKET_FORMAT="^([a-zA-Z0-9]+/)?([a-zA-Z]+)-([0-9]+)$"
